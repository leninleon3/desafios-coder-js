
let nombre = prompt ("ingrese su nombre")


for (let i = 0; i<=5; i++){
    if ((nombre === "sofia") || (nombre === "juan") || (nombre === "lucia") || (nombre === "ana") (nombre === "lucas")) {
        alert ("bienvenido" + nombre);
        else if() { alert ("usuario incorrecto");} 
        }

    if ( i === 5 ) {
        alert ("asistencia completa");
        else ( i != 5){
            alert ("asistencia incompleta")
        }
    }

    console.log ()



}